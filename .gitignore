# PlatformIO build output
.pio/
firmware/.pio/

# ESP-IDF generated config / build
firmware/build/
firmware/sdkconfig
firmware/sdkconfig.old
managed_components/

# Built sprite assets — produced by sprite_forge (architecture §8, §9).
# All of firmware/assets/baby + palettes is generated (.bin, .pal, copied
# anchors.json). The README inside firmware/assets is committed by hand.
firmware/assets/baby/
firmware/assets/palettes/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Editor / OS
.DS_Store
Thumbs.db
*.swp
*~

# VS Code ESP-IDF extension state (user-specific paths + caches)
.vscode/
firmware/.cache/

# Local device recovery images and host preview intermediates
backup/2026-09-19/*.bin
docs/previews/little-meadow/*.ppm

backup/vector-v1/*.bin

docs/previews/pixel-v1/*.ppm

releases/pixel-v1/*.bin

# Device-local Wi-Fi credentials and scoped pet token
firmware/components/voice/secrets.h
backup/pre-voice-v1/
docs/previews/voice-v1/*.ppm

# Per-device credentials and isolated firmware builds
firmware/private/
firmware/build-*/
