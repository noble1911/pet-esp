# firmware/assets

Built `.bin` sprite sheets live here. They are **generated** by
`sprite_forge` (architecture §8) and are **gitignored** — do not commit
them. This README keeps the directory in the tree.

Build them with:

```
cd ../../sprite_forge
python build.py
```

Format: see [docs/sprite_format.md](../../docs/sprite_format.md).
